from __future__ import annotations


def build_rf_detr_model(runtime_cfg: dict):
    raise NotImplementedError(
        "RF-DETR builder is not wired yet. Add the actual RF-DETR package import "
        "and model construction logic in src/model/rf_detr.py."
    )

